#include<iostream>
#include<cstring>
using namespace std;

int main()
{
	char str[200];
	int count=0;
	cin>>str;
	for(int i=0;str[i]!='\0';i++)
	{
		if(str[i]==32 || str[i]=='\n'|| str[i]=='\t')
		{
		        
			count++;
		}
		cout<<"hello";
	}
	cout<<count<<endl;
	return 0;
}
